import React from "react";

const Loader = () => (
  <div style={{ textAlign: "center", marginTop: "1rem" }}>
    <span style={{ fontSize: "1.2rem", color: "#555" }}>Loading...</span>
  </div>
);

export default Loader;
